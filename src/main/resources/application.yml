spring:
  application:
    name: melevanoronha
  
  datasource:
    url: jdbc:postgresql://localhost:5432/me-leva-noronha
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false
        use_sql_comments: true

server:
  port: 8080

jwt:
  secret: me-leva-noronha-secret-key-minimo-256-bits-para-seguranca-jwt-token-apenas-desenvolvimento
  expiration: 28800 
  refresh-expiration: 604800

weather:
  open-meteo:
    base-url: https://api.open-meteo.com/v1
    forecast-url: ${weather.open-meteo.base-url}/forecast
  location:
    latitude: -3.8548
    longitude: -32.4233
    timezone: America/Recife

flight:
  api:
    enabled: true
    base-url: https://test.api.amadeus.com/v2/shopping/flight-offers
    auth-url: https://test.api.amadeus.com/v1/security/oauth2/token
    key: IWsu4tTjxbsqKf5R3gYFuPlCJnD1juJp
    secret: hsKcFgkZAPCKtSAN
  fallback:
    price-per-person: 1500.0

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

